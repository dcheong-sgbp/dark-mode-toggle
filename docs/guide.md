# Guide: Why CSS variables

With variables, one data-theme attribute on the html element switches every colour at once — no per-component logic. It composes cleanly with prefers-color-scheme so you honour the system setting on arrival and let the toggle take over after.

## Beyond this tool

This is one of several free developer tools from
[SGBP — Singapore Build Partners](https://sgbp.tech). If you'd like a full website built or
audited for performance, accessibility and SEO rather than a single utility, that's what the
studio does for Singapore SMEs.
